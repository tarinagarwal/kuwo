# F0 — Payment-leg findings (Razorpay test mode)

**Question:** can this test-mode account take a payment to `captured` fully headlessly?

**Status:** ⏳ not yet run — waiting on test keys in `.env`. Run `pnpm probe:f0` and record the verdict here verbatim (including errors — this file feeds the Failure Recovery section).

## Probe results

*(paste probe output here after running)*

## Chosen payment-executor path

*(S2S JSON / scripted checkout / payment link + webhook — with the reason)*
